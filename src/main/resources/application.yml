spring:
  application:
    name: likelion-BE-weekly

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/likeliondb_mg?characterEncoding=UTF-8
    username: root
    password: wannagola12!@  # 실제 MySQL 비밀번호로 변경하세요

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        # use_sql_comments: true

logging:
  level:
    org:
      springframework:
        jdbc: DEBUG
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
        orm:
          jdbc:
            bind: TRACE
